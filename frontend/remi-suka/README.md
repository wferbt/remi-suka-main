# 🥦 GreenFood - Grocery Store App

Полнофункциональное приложение магазина продуктов с использованием **NestJS**, **React (Vite)** и **PostgreSQL**. Проект полностью контейнеризирован с помощью **Docker**.

## 🚀 Быстрый запуск

Для запуска проекта вам понадобится установленный [Docker](https://www.docker.com/products/docker-desktop/) и [Docker Compose](https://docs.docker.com/compose/install/).

1.  **Склонируйте проект или распакуйте архив**:
    ```bash
    cd remiblyat
    ```

2.  **Запустите проект**:
    Команда соберет образы и поднимет все сервисы (база данных, бэкенд, фронтенд):
    ```bash
    docker-compose up --build
    ```

3.  **Откройте приложение**:
    -   **Frontend**: [http://localhost:5173](http://localhost:5173)
    -   **Backend API**: [http://localhost:3000/catalog](http://localhost:3000/catalog)

---

## 🛠 Технологический стек

-   **Frontend**: React, Vite, Tailwind CSS, Axios.
-   **Backend**: NestJS, TypeORM, PostgreSQL.
-   **Infrastructure**: Docker, Docker Compose.

---

## 📦 Структура проекта

-   `/frontend` — Клиентская часть на React.
-   `/backend` — Серверная часть на NestJS.
-   `docker-compose.yml` — Файл конфигурации всех контейнеров.

## 📝 Примечания по базе данных

При первом запуске база данных будет пустой. Чтобы увидеть товары в каталоге, вы можете добавить тестовый продукт через терминал (пока запущен Docker):

```bash
docker exec -it grocery_db psql -U postgres -d grocery_db -c "INSERT INTO product (name, price, stock, \"externalId\") VALUES ('Зеленое яблоко', 120, 50, 'apple-001');"
